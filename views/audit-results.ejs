<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HubSpot Audit Results</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --hubspot-orange: #ff7a59;
            --hubspot-blue: #33475b;
            --hubspot-light-blue: #00a4bd;
            --success-green: #28a745;
            --warning-orange: #ffc107;
            --danger-red: #dc3545;
        }
        
        body {
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
            min-height: 100vh;
        }
        
        .hubspot-gradient {
            background: linear-gradient(135deg, var(--hubspot-orange) 0%, #ff5e7d 100%);
        }
        
        .score-circle {
            width: 200px;
            height: 200px;
            border: 8px solid;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            margin: 0 auto;
            position: relative;
            box-shadow: 0 8px 25px rgba(0,0,0,0.15);
        }
        
        .score-circle::before {
            content: '';
            position: absolute;
            width: 180px;
            height: 180px;
            border-radius: 50%;
            background: white;
            z-index: 1;
        }
        
        .score-content {
            position: relative;
            z-index: 2;
            text-align: center;
        }
        
        .score-value {
            font-size: 3.5rem;
            font-weight: 800;
            line-height: 1;
            margin-bottom: 0.25rem;
        }
        
        .score-label {
            font-size: 1rem;
            font-weight: 600;
            opacity: 0.8;
        }
        
        .section-card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
            transition: all 0.3s ease;
            height: 100%;
        }
        
        .section-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 30px rgba(0,0,0,0.12);
        }
        
        .progress {
            height: 12px;
            border-radius: 10px;
            background-color: #e9ecef;
        }
        
        .progress-bar {
            border-radius: 10px;
            transition: width 1s ease-in-out;
        }
        
        .badge-priority {
            font-size: 0.75rem;
            padding: 0.35em 0.65em;
        }
        
        .recommendation-item {
            border-left: 4px solid var(--hubspot-light-blue);
            background: linear-gradient(135deg, #f8fdff 0%, #ffffff 100%);
            border-radius: 0 8px 8px 0;
            transition: all 0.3s ease;
        }
        
        .recommendation-item:hover {
            border-left-color: var(--hubspot-orange);
            background: linear-gradient(135deg, #fff8f6 0%, #ffffff 100%);
        }
        
        .contact-card {
            background: linear-gradient(135deg, #ffffff 0%, #f8fdff 100%);
            border: 1px solid rgba(0, 164, 189, 0.2);
            border-radius: 15px;
        }
        
        .floating-action {
            position: fixed;
            bottom: 30px;
            right: 30px;
            z-index: 1000;
        }
        
        .pulse {
            animation: pulse 2s infinite;
        }
        
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }
        
        .section-icon {
            width: 50px;
            height: 50px;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            margin-right: 15px;
        }
    </style>
</head>
<body>
    <!-- Header -->
    <div class="hubspot-gradient text-white py-5 mb-5">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-8">
                    <h1 class="display-4 fw-bold mb-3">
                        <i class="fas fa-chart-line me-3"></i>HubSpot Portal Audit
                    </h1>
                    <p class="lead mb-4 opacity-90">
                        Comprehensive evaluation of <strong><%= totalPossible %></strong> key aspects across your HubSpot ecosystem
                    </p>
                    <div class="d-flex flex-wrap gap-2">
                        <span class="badge bg-light text-dark fs-6 py-2 px-3">
                            <i class="fas fa-star me-1"></i>Overall Score: <%= overallScore %>%
                        </span>
                        <span class="badge bg-light text-dark fs-6 py-2 px-3">
                            <i class="fas fa-check-circle me-1"></i><%= totalAchieved %>/<%= totalPossible %> Points
                        </span>
                    </div>
                </div>
                <div class="col-lg-4 text-center">
                    <div class="score-circle border-<%= overallScore < 40 ? 'danger' : overallScore < 70 ? 'warning' : overallScore < 85 ? 'success' : 'primary' %>">
                        <div class="score-content text-<%= overallScore < 40 ? 'danger' : overallScore < 70 ? 'warning' : overallScore < 85 ? 'success' : 'primary' %>">
                            <div class="score-value"><%= overallScore %>%</div>
                            <div class="score-label">HEALTH SCORE</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="container mb-5">
        <!-- Contact Information -->
        <div class="card contact-card border-0 shadow-sm mb-5">
            <div class="card-body">
                <div class="row align-items-center">
                    <div class="col-md-8">
                        <h5 class="card-title text-primary mb-3">
                            <i class="fas fa-user-circle me-2"></i>Report Generated For
                        </h5>
                        <div class="row">
                            <% if (first_name || last_name) { %>
                            <div class="col-sm-6 mb-2">
                                <strong><i class="fas fa-user me-2 text-muted"></i>Name:</strong>
                                <span class="text-dark"><%= first_name %> <%= last_name %></span>
                            </div>
                            <% } %>
                            <div class="col-sm-6 mb-2">
                                <strong><i class="fas fa-envelope me-2 text-muted"></i>Email:</strong>
                                <span class="text-dark"><%= email %></span>
                            </div>
                            <% if (company) { %>
                            <div class="col-sm-6 mb-2">
                                <strong><i class="fas fa-building me-2 text-muted"></i>Company:</strong>
                                <span class="text-dark"><%= company %></span>
                            </div>
                            <% } %>
                            <div class="col-sm-6 mb-2">
                                <strong><i class="fas fa-calendar me-2 text-muted"></i>Date:</strong>
                                <span class="text-dark"><%= new Date().toLocaleDateString() %></span>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-4 text-md-end">
                        <button onclick="window.print()" class="btn btn-outline-primary me-2">
                            <i class="fas fa-print me-1"></i>Print
                        </button>
                        <a href="/audit" class="btn btn-primary">
                            <i class="fas fa-redo me-1"></i>Retake Audit
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Executive Summary -->
        <div class="card border-0 shadow-sm mb-5">
            <div class="card-body">
                <h4 class="card-title text-primary mb-4">
                    <i class="fas fa-chart-pie me-2"></i>Executive Summary
                </h4>
                <div class="row">
                    <div class="col-lg-8">
                        <p class="fs-5 mb-4">
                            <% if (overallScore < 40) { %>
                                <span class="text-danger fw-bold">Foundational Level:</span> Your organization is in the early stages of HubSpot adoption. Focus on foundational setup, basic configuration, and team training to build a solid platform for growth.
                            <% } else if (overallScore < 70) { %>
                                <span class="text-warning fw-bold">Intermediate Level:</span> You have established a good foundation with HubSpot. The next phase involves optimizing automation, improving data quality, and enhancing reporting capabilities.
                            <% } else if (overallScore < 85) { %>
                                <span class="text-success fw-bold">Advanced Level:</span> Your organization demonstrates advanced HubSpot usage. Consider exploring AI features, advanced integrations, and optimization strategies to maximize ROI.
                            <% } else { %>
                                <span class="text-primary fw-bold">Elite Level:</span> Your organization exemplifies HubSpot excellence. Focus on maintaining best practices, exploring new features, and optimizing team adoption.
                            <% } %>
                        </p>
                        <div class="d-flex flex-wrap gap-3">
                            <div class="text-center">
                                <div class="fs-2 fw-bold text-primary"><%= overallScore %>%</div>
                                <small class="text-muted">Overall Score</small>
                            </div>
                            <div class="text-center">
                                <div class="fs-2 fw-bold text-success"><%= totalAchieved %></div>
                                <small class="text-muted">Points Achieved</small>
                            </div>
                            <div class="text-center">
                                <div class="fs-2 fw-bold text-info"><%= totalPossible %></div>
                                <small class="text-muted">Total Possible</small>
                            </div>
                        </div>
                    </div>
                    <div class="col-lg-4">
                        <div class="card bg-light border-0">
                            <div class="card-body text-center">
                                <i class="fas fa-lightbulb fa-3x text-warning mb-3"></i>
                                <h6 class="fw-bold">Key Insight</h6>
                                <p class="small text-muted mb-0">
                                    <% if (overallScore < 70) { %>
                                        Focus on high-impact areas first to maximize your HubSpot ROI
                                    <% } else { %>
                                        Optimize advanced features to maintain competitive advantage
                                    <% } %>
                                </p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Section Breakdown -->
        <h3 class="mb-4 text-primary">
            <i class="fas fa-th-large me-2"></i>Section Performance Breakdown
        </h3>
        <div class="row g-4 mb-5">
            <% Object.keys(sectionScores).forEach(section => { 
                const sectionData = sectionScores[section];
                const percentage = sectionData.percentage;
                
                let progressClass = 'bg-danger';
                let badgeClass = 'bg-danger';
                let badgeText = 'High Priority';
                let iconClass = 'fas fa-exclamation-triangle text-danger';
                
                if (percentage < 40) {
                    progressClass = 'bg-danger';
                    badgeClass = 'bg-danger';
                    badgeText = 'High Priority';
                    iconClass = 'fas fa-exclamation-triangle text-danger';
                } else if (percentage < 70) {
                    progressClass = 'bg-warning';
                    badgeClass = 'bg-warning text-dark';
                    badgeText = 'Growth Area';
                    iconClass = 'fas fa-chart-line text-warning';
                } else if (percentage < 85) {
                    progressClass = 'bg-success';
                    badgeClass = 'bg-success';
                    badgeText = 'Strong Area';
                    iconClass = 'fas fa-check-circle text-success';
                } else {
                    progressClass = 'bg-primary';
                    badgeClass = 'bg-primary';
                    badgeText = 'Excellent';
                    iconClass = 'fas fa-trophy text-primary';
                }
            %>
            <div class="col-xl-4 col-lg-6">
                <div class="card section-card h-100">
                    <div class="card-body">
                        <div class="d-flex align-items-center mb-3">
                            <div class="section-icon bg-light text-primary">
                                <i class="<%= iconClass %>"></i>
                            </div>
                            <div class="flex-grow-1">
                                <h6 class="card-title mb-1 fw-bold text-dark"><%= section %></h6>
                                <div class="text-muted small">
                                    <%= sectionData.score %>/<%= sectionData.max %> points
                                </div>
                            </div>
                            <span class="badge <%= badgeClass %> badge-priority">
                                <%= badgeText %>
                            </span>
                        </div>
                        
                        <div class="progress mb-3">
                            <div class="progress-bar <%= progressClass %>" 
                                 role="progressbar" 
                                 style="width: <%= percentage %>%"
                                 aria-valuenow="<%= percentage %>" 
                                 aria-valuemin="0" 
                                 aria-valuemax="100">
                            </div>
                        </div>
                        
                        <div class="d-flex justify-content-between align-items-center">
                            <span class="fw-bold text-<%= percentage < 40 ? 'danger' : percentage < 70 ? 'warning' : percentage < 85 ? 'success' : 'primary' %>">
                                <%= percentage %>%
                            </span>
                            <small class="text-muted">
                                <% if (percentage < 40) { %>
                                    Needs attention
                                <% } else if (percentage < 70) { %>
                                    Good foundation
                                <% } else if (percentage < 85) { %>
                                    Strong performance
                                <% } else { %>
                                    Excellent work!
                                <% } %>
                            </small>
                        </div>
                    </div>
                </div>
            </div>
            <% }) %>
        </div>

       

        <!-- Call to Action -->
        <div class="card border-0 shadow-sm bg-primary text-white">
            <div class="card-body text-center py-5">
                <h3 class="card-title mb-3">Ready to Optimize Your HubSpot?</h3>
                <p class="card-text mb-4 opacity-90">
                    Get personalized help implementing these recommendations and maximizing your ROI
                </p>
                <div class="d-flex flex-wrap justify-content-center gap-3">
                    <a href="mailto:help@youraudit.com?subject=HubSpot%20Audit%20Consultation&body=Hi,%20I%20completed%20the%20HubSpot%20audit%20with%20a%20score%20of%20<%= overallScore %>%" 
                       class="btn btn-light btn-lg px-4">
                        <i class="fas fa-envelope me-2"></i>Email Our Team
                    </a>
                    <a href="/consultation" class="btn btn-outline-light btn-lg px-4">
                        <i class="fas fa-calendar me-2"></i>Book Free Consultation
                    </a>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="bg-dark text-light py-4 mt-5">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-md-6">
                    <p class="mb-0">
                        <i class="fas fa-shield-alt me-2"></i>HubSpot Portal Audit Dashboard
                    </p>
                </div>
                <div class="col-md-6 text-md-end">
                    <p class="mb-0">
                        Generated on <%= new Date().toLocaleDateString() %> 
                        <a href="/" class="text-light ms-3"><i class="fas fa-home me-1"></i>Home</a>
                    </p>
                </div>
            </div>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        // Animate progress bars on page load
        document.addEventListener('DOMContentLoaded', function() {
            setTimeout(function() {
                const progressBars = document.querySelectorAll('.progress-bar');
                progressBars.forEach(bar => {
                    const width = bar.style.width;
                    bar.style.width = '0%';
                    setTimeout(() => {
                        bar.style.width = width;
                    }, 100);
                });
            }, 500);
        });
    </script>
</body>
</html>